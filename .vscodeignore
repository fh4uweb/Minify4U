.vscode/**
.vscode-test/**
src/**
out/**
test-workspace*/**
*.code-workspace
.gitignore
.git/**
esbuild.js
tsconfig.json
tools/**
**/*.map
**/*.ts

# Minify4U minifiziert seit 0.5.0 den eigenen esbuild-Output: der Watcher sieht
# dist/extension.js entstehen und legt extension.min.js daneben. Geladen wird nur
# extension.js ("main"), die Minifizierte ist reiner Ballast im Paket.
dist/*.min.js
_CHANGELOG-MINIFY4U.md
CLAUDE.md

# node_modules NICHT ignorieren: sass/less/html-minifier-terser/jsonc-parser sind
# external und müssen mit ausgeliefert werden. vsce entfernt devDependencies selbst.
