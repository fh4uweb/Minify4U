{
  "name": "minify4u",
  "displayName": "Minify4U",
  "description": "Minify source files (JS/CSS/SCSS/SASS/LESS/HTML/JSON) on save and write the output to a path that is freely configurable per file type.",
  "version": "0.3.0",
  "publisher": "4uweb",
  "license": "MIT",
  "icon": "images/minify4u.png",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Formatters",
    "Other"
  ],
  "keywords": [
    "minify",
    "terser",
    "sass",
    "scss",
    "less",
    "css"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/fh4uweb/Minify4U.git"
  },
  "bugs": {
    "url": "https://github.com/fh4uweb/Minify4U/issues"
  },
  "homepage": "https://github.com/fh4uweb/Minify4U#readme",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Minify4U",
      "properties": {
        "minify4u.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable Minify4U on save.",
          "scope": "resource"
        },
        "minify4u.output.javascript": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **JavaScript** (terser), relative to the folder root.\n\nExample: `assets/js` — saving `src/app.js` writes `assets/js/app.min.js`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.css": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **CSS** (clean-css), relative to the folder root.\n\nExample: `assets/css` — saving `src/style.css` writes `assets/css/style.min.css`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.scss": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **SCSS** — compiled *and* minified (Dart Sass), relative to the folder root.\n\nExample: `assets/css` — saving `src/style.scss` writes `assets/css/style.min.css`.\n\n`*` = write next to the source file · empty = disabled (leave empty if a dedicated Sass compiler already handles SCSS)",
          "scope": "resource"
        },
        "minify4u.output.sass": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **indented Sass** (`.sass`) — compiled *and* minified (Dart Sass), relative to the folder root.\n\nExample: `assets/css` — saving `src/style.sass` writes `assets/css/style.min.css`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.less": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **LESS** — compiled *and* minified, relative to the folder root.\n\nExample: `assets/css` — saving `src/theme.less` writes `assets/css/theme.min.css`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.html": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **HTML** (html-minifier-terser), relative to the folder root.\n\nExample: `assets/html` — saving `src/page.html` writes `assets/html/page.min.html`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.json": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **JSON** (compact), relative to the folder root.\n\nExample: `assets/json` — saving `src/data.json` writes `assets/json/data.min.json`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.output.jsonc": {
          "type": "string",
          "default": "",
          "markdownDescription": "Output folder for **JSONC** (JSON with comments; comments are removed), relative to the folder root.\n\nExample: `assets/json` — saving `src/config.jsonc` writes `assets/json/config.min.json`.\n\n`*` = write next to the source file · empty = disabled",
          "scope": "resource"
        },
        "minify4u.rules": {
          "type": "array",
          "default": [],
          "markdownDescription": "Rules per file type/glob. On save the **first** matching rule applies; if none matches, the `minify4u.output.*` setting for that language is used.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "glob": {
                "type": "string",
                "markdownDescription": "Glob relative to the folder root, e.g. `src/js/**/*.js`. Alternative to `type`."
              },
              "type": {
                "type": "string",
                "markdownDescription": "VS Code language ID, e.g. `javascript`, `css`. Alternative to `glob`."
              },
              "savePath": {
                "type": "string",
                "minLength": 1,
                "markdownDescription": "Target folder relative to the folder root, e.g. `assets/js`. Use `*` to write next to the source file."
              },
              "suffix": {
                "type": "string",
                "markdownDescription": "Output file extension, e.g. `.min.js`. Replaces the original extension.",
                "default": ".min.js"
              },
              "minifier": {
                "type": "string",
                "enum": [
                  "terser",
                  "clean-css",
                  "sass",
                  "less",
                  "html",
                  "json",
                  "json-pretty"
                ],
                "enumDescriptions": [
                  "Minify JavaScript (Terser).",
                  "Minify CSS (clean-css).",
                  "Compile + minify SCSS/SASS → CSS (Dart Sass, compressed).",
                  "Compile + minify LESS → CSS (less + clean-css).",
                  "Minify HTML (html-minifier-terser).",
                  "Minify JSON/JSONC (comments/whitespace removed, compact).",
                  "Convert JSON/JSONC to readable JSON (comments/trailing commas removed, indented) — not minified."
                ],
                "markdownDescription": "Minifier: `terser` (JS), `clean-css` (CSS), `sass` (SCSS/SASS→CSS), `less` (LESS→CSS), `html`, `json` (compact), `json-pretty` (readable JSON, not minified)."
              }
            },
            "required": [
              "savePath",
              "suffix",
              "minifier"
            ]
          },
          "scope": "resource"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "package": "vsce package"
  },
  "dependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "jsonc-parser": "^3.3.1",
    "less": "^4.6.7",
    "sass": "^1.101.0",
    "terser": "^5.31.0"
  },
  "devDependencies": {
    "@types/clean-css": "^4.2.11",
    "@types/less": "^3.0.8",
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
